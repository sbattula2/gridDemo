import React from "react";
import Typography from "@mui/material/Typography";

const HomePage: React.FC = () => {
  return (
    <Typography variant="h5">
      Click one of the example links above ↑ to get started!
    </Typography>
  );
};

export default HomePage;
